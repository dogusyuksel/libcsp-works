# How To Use

You only need to use "make"

Valid commands;

* > make create-bindings     --> to create bindings
* > make build-server        --> to build libcsp server
* > make console             --> login to image to play around

